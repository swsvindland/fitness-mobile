import 'package:body_track/widgets/checkin_list.dart';
import 'package:flutter/material.dart';

class All extends StatelessWidget {
  const All({super.key});

  @override
  Widget build(BuildContext context) {
    return const CheckInList();
  }
}
